import React, { memo } from "react";
import CommonStyleForm from "../../_common/CommonStyleForm";

const PictureStyleForm = memo(() => {
  return <CommonStyleForm />;
});

export default PictureStyleForm;
