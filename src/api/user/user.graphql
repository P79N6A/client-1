
# 用户注册
mutation Register($phone: String!, $password: String!, $verification: String!) {
  register(phone: $phone, verification: $verification, password: $password) {
    error
    msg
  }
}

# 发送短信
mutation Sms($phone: String!) {
  sms(phone: $phone) {
    error
    msg
  }
}
