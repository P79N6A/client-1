# 用户注册
mutation Login($phone: String!, $password: String!, $verify: String!,$rememberMe:Boolean) {
  login(phone: $phone,  password: $password,verify: $verify,rememberMe:$rememberMe) {
    error
    msg
  }
}
