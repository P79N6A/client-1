export interface ITheme {
  // 主题
  theme: {
    color: string;
    background: string;
  };
  //背景色
  background: string;
}

const $$Theme = {
  theme: {
    color: '',
    background: '',
  },
  background: '',
};

export default $$Theme;
