---
name: swiper
route: /swiper
menu: 📃 applet
---

import Swiper from "../../../src/page/applet/library/swiper/Swiper";
import { $$swiper } from "../../../src/page/applet/library/swiper/database";

# swiper component

## 使用

```
  <Swiper {...data}/>
```

## 示例

<Swiper {...$$swiper()} width={"400px"} height={"300px"} />

## 接口

```
  type?: string;
  width?: string; // PC端样式问题，必须要有
  height?: string; // PC端样式问题，必须要有
  item?: Array<{
    img?: string;
    link?: {
      use?: string;
      map?: number[];
      href?: string;
      phone?: string;
    };
  }>;
  autoPlay?: true;
```
